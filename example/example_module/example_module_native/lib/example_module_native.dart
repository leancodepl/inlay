// Companion plugin for example_module.
//
// This package exists solely to hold generated native code (Routes, Stores)
// so that `flutter build aar` / `flutter build swift-package` bundle them into
// binary artifacts.
// There is no Dart API — import the generated Dart code from example_module
// instead.
