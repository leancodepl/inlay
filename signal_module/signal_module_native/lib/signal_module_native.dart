// Companion plugin for signal_module.
//
// This package exists solely to hold generated native code (Routes, Stores)
// so that `flutter build aar` bundles them into binary artifacts.
// There is no Dart API — import the generated Dart code from signal_module
// instead.
