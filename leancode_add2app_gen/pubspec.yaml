name: leancode_add2app_gen
description: Code generator for add2app routes and stores. Supports both CLI and build_runner.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.8

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  args: ^2.7.0
  analyzer: ^7.0.0
  yaml: ^3.1.2
  build: ^2.4.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  build_test: ^2.2.2
