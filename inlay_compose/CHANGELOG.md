# 0.2.0

- Version bump to stay in lockstep with `inlay` 0.2.0; no changes

# 0.1.1

- Documentation and example improvements

# 0.1.0

- Initial release
- `InlayFlutterScreen` - embed an inlay Flutter page as a Jetpack Compose destination, with a typed `onResult` callback
- `InlayFlutterDialog` - state-driven Flutter dialog presentation over Compose screens, with `onDismissRequest` and a typed `onResult` callback
