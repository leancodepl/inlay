name: inlay_gen
description: Code generator for inlay routes and stores. Supports both CLI and build_runner.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  analyzer: ^10.0.0
  args: ^2.7.0
  build: ^4.0.0
  path: ^1.9.0
  yaml: ^3.1.2

dev_dependencies:
  build_test: ^3.0.0
  leancode_lint: ^21.0.0
  test: ^1.25.6
